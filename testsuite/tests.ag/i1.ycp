//
{
    integer old = WFM::SCRGetDefault ();

    integer h = WFM::SCROpen ("ag_netd", false);
    if (h < 1)
    {
	return;
    }
    WFM::SCRSetDefault (h);
    SCR::Netd (WFM::Args (0));
    list r = SCR::Dir (.);

    WFM::SCRClose (h);
    WFM::SCRSetDefault (old);

    return r;
}
