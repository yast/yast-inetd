// #24379, changing consecutive services
{
    SCR::RegisterAgent(.test.inetd, `ag_netd (`Xinetd (WFM::Args (0))));

    list s = (list) SCR::Read (.test.inetd.services);
    s[0, "enabled"] = false;
    s[0, "changed"] = true;
    s[1, "enabled"] = false;
    s[1, "changed"] = true;
    boolean r = (boolean) SCR::Write (.test.inetd.services, s);

    return r;
}
