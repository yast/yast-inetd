$Id$

* Files created by user:
foo.ycp:	The main file of a test case
foo.in:		What the agent parses
foo.err:	Should be empty
foo.out:	How the concatenated output of foo.ycp and foo.in.test
		should look like
foo.bar.d.in:	Input files for testing includedir, placed in tmp/idir
foo.bar.d.out:	Expected output for them

* Files created during run:
foo.in.test:	What the agent really parses and then writes into
